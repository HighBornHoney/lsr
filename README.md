## Порядок запуска проекта

- клонировать репозиторий `git clone https://github.com/HighBornHoney/lsr.git`
- перейти в корень проекта `cd lsr`
- скопировать файл с примером конфигурации и установить необходимые настройки `cp .env.example .env`
- запустить докер контейнеры `docker compose up -d`
